[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "integration: marks tests that require the service running (deselect with '-m \"not integration\"')",
]
asyncio_mode = "auto"
log_cli_level = "INFO"
