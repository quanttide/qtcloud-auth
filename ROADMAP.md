# ROADMAP · 量潮身份云

构建支持**手机验证码登录**的管理后台，从当前的原型逐步演化为生产可用的身份云服务。

---

## 里程碑

### P0 — 手机验证码核心链路 ✅

支撑"输入手机号 → 获取验证码 → 验证码登录"的闭环。

| 阶段 | 任务 | 状态 |
|------|------|------|
| 0.1 | **User 模型扩展** — `phone`、`phone_verified`、`nickname`、`avatar` 字段 | ✅ |
| 0.2 | **验证码存储** — `VerificationCode` 模型：`phone`, `code`, `expires_at`, `used` | ✅ |
| 0.3 | **SMS 服务抽象** — `SMSSender` 接口，内置 `ConsoleSender` | ✅ |
| 0.4 | **发送验证码 API** — `POST /oauth/sms/send`，频率限制，生成 6 位码 | ✅ |
| 0.5 | **验证码登录 API** — `POST /oauth/token` `grant_type=sms_code`，自动注册/登录，返回 JWT | ✅ |
| 0.6 | **管理后台账号体系** — admin 种子用户、`EnsureAdmin` 幂等初始化 | ✅ |

**交付物**: 可用 Postman/cURL 完成"手机号 → 验证码 → 登录 → 访问受保护接口"全流程。✅

---

### P0+ — 基础工程（已完成）

| 任务 | 说明 |
|------|------|
| 服务入口 | `main.go` HTTP 服务，BuntDB 内存存储，支持环境变量配置 |
| 集成测试 | Python pytest 11 项覆盖所有端点（密码/短信/刷新/用户信息/错误场景） |
| 文档 | 用户指南、开发运维指南、API 参考 |
| 发布 | `v0.0.1`、`provider/v0.0.1` GitHub Release |

---

### P1 — 管理后台接口

面向运营/管理员的后台 REST API。

| 阶段 | 任务 | 说明 |
|------|------|------|
| 1.1 | **角色权限管理** | CRUD Role，支持自定义权限列表 |
| 1.2 | **用户管理** | 管理员视角：查看用户列表、详情、禁用/启用、重置手机号 |
| 1.3 | **验证码配置** | 有效期、重发间隔、每日上限等可配置 |
| 1.4 | **操作日志** | 记录管理员操作、登录事件、验证码发送事件 |

---

### P2 — 持久化与生产化

摆脱内存存储，对接真实基础设施。

| 阶段 | 任务 | 说明 |
|------|------|------|
| 2.1 | **SQLite 实现 Storer** | 本地开发/单机部署，零依赖 |
| 2.2 | **MySQL/PostgreSQL 实现 Storer** | 生产部署 |
| 2.3 | **Redis 验证码缓存** | 验证码走 Redis TTL，自动过期，支持分布式 |
| 2.4 | **真实 SMS 通道** | 接入阿里云/腾讯云/七牛云短信 SDK |

---

### P3 — 安全与运维

| 阶段 | 任务 | 说明 |
|------|------|------|
| 3.1 | **频率限制** | 验证码发送、登录失败 IP/手机号级别限流 |
| 3.2 | **JWT 改进** | 支持 RS256 非对称签名、refresh token 轮换机制 |
| 3.3 | **配置中心** | 环境变量 / YAML 配置文件统一管理 |
| 3.4 | **健康检查 & 指标** | `/healthz`、Prometheus 指标暴露 |
| 3.5 | **Docker 镜像** | 多阶段构建，生产镜像 < 20MB |

---

### P4 — 管理后台前端（可选）

| 阶段 | 任务 | 说明 |
|------|------|------|
| 4.1 | **基础框架** | React / Vue SPA，登录页，路由鉴权 |
| 4.2 | **用户管理页面** | 列表、搜索、详情、编辑 |
| 4.3 | **角色管理页面** | 角色创建、权限勾选 |
| 4.4 | **验证码记录查询** | 发送记录、登录记录 |

---

## 当前状态

```
P0  ████████████ 100%  手机验证码核心链路
P0+ ████████████ 100%  基础工程（服务入口 / 集成测试 / 文档 / 发布）
P1  ░░░░░░░░░░░░   0%  管理后台接口
P2  ░░░░░░░░░░░░   0%  持久化与生产化
P3  ░░░░░░░░░░░░   0%  安全与运维
P4  ░░░░░░░░░░░░   0%  管理后台前端
```

---

## 技术栈

| 层 | 选型 |
|----|------|
| 语言 | Go 1.23 |
| HTTP | `net/http` + 标准库 |
| 数据 | `Storer` 接口 → BuntDB（开发）/ SQLite / MySQL / PostgreSQL |
| 集成测试 | Python 3.12 + pytest + httpx |
| 缓存 | Redis（验证码 TTL） |
| JWT | HS256 → RS256 |
| 部署 | Docker / Docker Compose |
